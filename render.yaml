services:
  - type: web
    runtime: static
    name: AI-Code-Review-Assistant-2

    buildCommand: pnpm install && pnpm --filter @workspace/ai-code-review build
    staticPublishPath: artifacts/ai-code-review/dist/public

    routes:
      - type: rewrite
        source: /*
        destination: /index.html